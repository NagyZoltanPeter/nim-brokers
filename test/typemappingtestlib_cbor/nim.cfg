# Project-level config for the typemappingtestlib_cbor DLL build.
# See test/typemappingtestlib/nim.cfg for the rationale on the
# Windows+GCC linker workaround.
@if windows:
  @if gcc:
    gcc.options.linker = "-lpthread"
  @end
@end
