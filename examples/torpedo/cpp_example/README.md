# C++ Consumer

Planned home of an optional C++ consumer for the Torpedo Duel example.

This is expected to be a second-stage addition after the Nim backend and Python
text UI are working.

Its role is to demonstrate that the same Broker FFI API surface can be consumed
through the generated modern C++ wrapper without changing the backend design.